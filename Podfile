# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'SleepTracker-Swift' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for SleepTracker-Swift
  pod 'DatePickerCell'
  
  target 'SleepTracker-SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SleepTracker-SwiftUITests' do
    # Pods for testing
  end

end
